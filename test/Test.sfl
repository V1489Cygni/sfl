List = Empty | Cons Integer List

put :: Integer -> List -> List
put a b = (Cons a b)

top :: List -> Integer
top (Cons a b) = a

next :: List -> List
next (Cons a b) = b

sum :: List -> Integer
sum Empty = 0
sum (Cons a b) = (Int.add a (sum b))

main = (sum (put 5 (next (put 7 (put 3 Empty)))))