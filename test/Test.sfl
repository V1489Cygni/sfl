module Test

import Data.List

fib :: Integer -> Integer
fib 0 = 1
fib 1 = 1
fib n = + (fib (- n 1)) (fib (- n 2))

div :: Integer -> Integer -> Integer
div a b = / a b

swap :: (Integer -> Integer -> Integer) -> Integer -> Integer -> Integer
swap f a b = f b a

list :: List
list = map (swap div 10) (put (fib 30) (put 20 Empty))

main :: String
main = + "Result: " (str list)